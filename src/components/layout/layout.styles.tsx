import { StyleSheet } from 'react-native';
import { Colors } from '@constants';

const LayoutStyles = StyleSheet.create({
  wrapper: { flex: 1, backgroundColor: Colors.bg.dark },
});

export default LayoutStyles;
